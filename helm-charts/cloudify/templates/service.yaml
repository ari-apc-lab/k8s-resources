apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudify.fullname" . }}
  labels:
    {{- include "cloudify.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "cloudify.selectorLabels" . | nindent 4 }}
